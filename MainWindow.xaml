<Window x:Class="TemporalMotionExtractionAnalysis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TemporalMotionExtractionAnalysis"
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="800">
    <Grid>

        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <Label Content="Selected Folder Name: " Margin="10, 20, 5, 20" />
                <Label Content="{Binding FolderName}" Margin="10, 20, 5, 20"/>
                <Button Content="Load Images" Command="{Binding LoadImagesCommand}" Margin="20"/>
            </StackPanel>
                        
            <Image Source="{Binding CurrentImage.ImagePath}" Height="350" Width="350"/>

            <!-- Timeline thumbnails -->
            <ListBox Grid.Row="1" ItemsSource="{Binding Images}" SelectionMode="Single">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding ImagePath}" Width="100" Height="100" Margin="5"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Orientation="Horizontal">
                <Button Content="Start Animation" Command="{Binding StartAnimationCommand}" Margin="20, 10"/>
                <Button Content="Stop Animation" Command="{Binding StopAnimationCommand}" Margin="20, 10"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>
