<Window x:Class="TemporalMotionExtractionAnalysis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:TemporalMotionExtractionAnalysis.Converters"
        xmlns:controls="clr-namespace:TemporalMotionExtractionAnalysis.Controls"
        xmlns:local="clr-namespace:TemporalMotionExtractionAnalysis.ViewModel"
        xmlns:models="clr-namespace:TemporalMotionExtractionAnalysis.Model"
        mc:Ignorable="d"
        Title="DECKER (Dynamic Extraction of Camouflaged Kinetics for Enhanced Recognition)" 
        Width="1400"
        Height="960"
        Background="Gray"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- Data Template Resources -->
        <DataTemplate x:Key="SecondItemTemplate">
            <StackPanel Orientation="Horizontal">
                <Image Source="{Binding ImagePath}" Height="Auto" Width="350" Margin="30,2,0,0"
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="DefaultTemplate">
            <StackPanel Orientation="Horizontal">
                <!-- Empty template for non-second items -->
            </StackPanel>
        </DataTemplate>

        <!-- Converter Resources -->
        <models:IndexBasedTemplateSelector x:Key="IndexBasedTemplateSelector"
                                  SecondItemTemplate="{StaticResource SecondItemTemplate}"
                                  DefaultTemplate="{StaticResource DefaultTemplate}"/>
        <converters:IndexConverter x:Key="IndexConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:FrameIndexVisibilityConverter x:Key="FrameIndexVisibilityConverter"/>
        <converters:FrameIndexTickVisibilityConverter x:Key="FrameIndexTickVisibilityConverter"/>
        <converters:SelectedFramesMultiValueConverter x:Key="SelectedFramesMultiValueConverter"/>
        <converters:StringToIntConverter x:Key="StringToIntConverter"/>
        <converters:RadioButtonConverter x:Key="RadioButtonConverter"/>
        <converters:StringToColorConverter x:Key="StringToColorConverter"/>
        <converters:GlyphConverter x:Key="GlyphConverter" />
        <converters:GlyphConverter x:Key="AreaSizeConverter" />
    </Window.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <!-- Parent Control Pane : Left -->
            <StackPanel Orientation="Vertical" Margin="5,5,0,0">

                <!-- Folder Loader Controls -->
                <StackPanel Orientation="Horizontal"  HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5,5,0,5" Width="250" Height="70">
                        <controls:FolderLoaderWidget/>
                    </Border>
                </StackPanel>

                <!-- Playback Controls Border with Label -->
                <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5" Height="130">
                    <controls:PlaybackControlsWidget/>
                </Border>

                <!-- Indication Frame Preview Pane -->
                <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5" Width="250" Height="220">
                    <controls:IndicationFrameWidget/>
                </Border>

                <!-- Foreground Visual Encoding Options Border with Label -->
                <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5" Width="250" Height="209">
                    <controls:ForegroundVisualEncodingWidget/>
                </Border>

                <!-- Background Visual Encoding Options Border with Label -->
                <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5" Width="250" Height="217">
                    <controls:BackgroundVisualEncodingWidget/>
                </Border>

            </StackPanel>

            <!-- Parent Control Pane : Center/Right -->
            <StackPanel Orientation="Vertical" Margin="0,5,0,0">
                <!-- Timeline Overview -->
                <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5" Width="1100" Height="70">
                    <controls:TimelineOverviewWidget/>
                </Border>

                <!-- Zoomed Timeline View -->
                <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5" Width="1100" Height="130">
                    <controls:ZoomedTimelineWidget/>
                </Border>

                <StackPanel Orientation="Horizontal">
                    <!-- Animation Frame -->
                    <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5" Width="545" Height="220">
                        <controls:AnimationPaneWidget/>
                    </Border>

                    <!-- Offset Frame -->
                    <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5" Width="545" Height="220">
                        <controls:OffsetPaneWidget/>
                    </Border>
                </StackPanel>

                <!-- Parent Pane: Bottom Center/Right -->
                <StackPanel Orientation="Horizontal">
                    <!-- Current Frame Transforms-->
                    <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5" Width="200" Height="435">
                        <controls:CurrentFrameTransformsWidget/>
                    </Border>

                    <!-- Composite View -->
                    <Border BorderBrush="DarkGray" BorderThickness="1" Margin="22" Width="640" Height="419">
                        <controls:CompositeViewWidget/>
                    </Border>

                    <!-- Offset Frame Transforms-->
                    <StackPanel Orientation="Vertical">
                        <Border BorderBrush="DarkGray" BorderThickness="1" Margin="5" Width="205" Height="435">
                            <controls:OffsetFrameTransformsWidget/>
                        </Border>
                    </StackPanel>

                </StackPanel>

            </StackPanel>
        </StackPanel>

        <!-- Arrows -->
        <TextBlock FontFamily="Segoe UI Symbol"
                   FontSize="40"
                   FontWeight="Bold"
                   Text="&#x27F1;"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Foreground="Black" Margin="626,433,0,0"/>

        <TextBlock FontFamily="Segoe UI Symbol"
                   FontSize="40"
                   FontWeight="Bold"
                   Text="&#x27F1;"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Foreground="Black" Margin="1005,433,0,0"/>

        <TextBlock FontFamily="Segoe UI Symbol"
                   FontSize="40"
                   FontWeight="Bold"
                   Text="&#x2B46;"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Foreground="Black" Margin="465,660,0,0"/>

        <TextBlock FontFamily="Segoe UI Symbol"
                   FontSize="40"
                   FontWeight="Bold"
                   Text="&#x2B45;"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Foreground="Black" Margin="1133,666,0,0"/>
    </Grid>
</Window>
