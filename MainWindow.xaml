<Window x:Class="TemporalMotionExtractionAnalysis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TemporalMotionExtractionAnalysis"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <StackPanel>
            <Image Source="{Binding CurrentImage.ImagePath}" Height="300" Width="300"/>
            <Button Content="Load Images" Command="{Binding LoadImagesCommand}"/>
            <Button Content="Start Animation" Command="{Binding StartAnimationCommand}"/>
            <Button Content="Stop Animation" Command="{Binding StopAnimationCommand}"/>
        </StackPanel>

    </Grid>
</Window>
