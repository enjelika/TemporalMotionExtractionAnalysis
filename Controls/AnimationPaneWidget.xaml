<UserControl x:Class="TemporalMotionExtractionAnalysis.Controls.AnimationPaneWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TemporalMotionExtractionAnalysis.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="220" d:DesignWidth="545">
    <Grid>
        <DockPanel Background="White">
            <TextBlock DockPanel.Dock="Top" Text="Current Frame" FontWeight="Bold" HorizontalAlignment="Left" Margin="20,10,0,0"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,-18,-8,13">

                <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center"  Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Label FontWeight="Bold" Content="Frame Index: "/>
                        <TextBlock VerticalAlignment="Center" Text="{Binding DataContext.CurrentIndex, RelativeSource={RelativeSource AncestorType=Window}}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                        <Label Content="Total Frames: " FontWeight="Bold"/>
                        <TextBlock VerticalAlignment="Center" Text="{Binding DataContext.ImageCount, RelativeSource={RelativeSource AncestorType=Window}}" />
                    </StackPanel>
                </StackPanel>

                <!-- Animation of images in selected folder -->
                <Image Source="{Binding CurrentImage.ImagePath}" Height="Auto" Width="400" Margin="0"/>
            </StackPanel>

        </DockPanel>
    </Grid>
</UserControl>
